package com.strings.samelength;

public class SameLength {
	
	//Comparing the strings
	public void compare(String s1,String s2) {
		if(s1.length()==s2.length())System.out.println("Both strings are of same length");
		else System.out.println("Both strings are of different length");
	}
}
